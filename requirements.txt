Django>=5.2,<6.0
djangorestframework>=3.18,<4.0
django-cors-headers>=4.9,<5.0
Pillow>=10.0
requests>=2.32
# Нужна для проверки подписи Google ID-токена.
cryptography>=42.0
# Проверка MX-записи почтового домена при регистрации.
dnspython>=2.6
# Фоновая очередь генерации. Без CELERY_BROKER_URL задача выполняется
# прямо в запросе — так работают тесты и локальная разработка под Windows,
# где нет ни Redis, ни нормального воркера.
celery[redis]>=5.4,<6.0
